<h1><%= @author.name %></h1>

<ul>
  <% if @books.present? %>
    <% @books.each do |book| %>
      <li><%= link_to book.name, book_path(book) %></li>
    <% end %>
  <% else %>
    <li>No books found for this author</li>
  <% end %>
</ul>
